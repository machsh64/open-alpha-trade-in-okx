{
  "name": "open-alpha-arena",
  "private": true,
  "scripts": {
    "dev": "node scripts/detect-platform.js",
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:backend:win": "cd backend && run_dev.bat",
    "dev:backend:unix": "cd backend && bash run_dev.sh",
    "install:all": "pnpm install && node scripts/setup-python.js",
    "build": "pnpm run build:frontend && pnpm run build:backend",
    "build:frontend": "cd frontend && pnpm build",
    "build:backend": "cd backend && echo \"Backend build step not yet implemented\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  }
}
